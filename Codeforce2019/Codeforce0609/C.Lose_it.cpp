#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    scanf("%d",&n);
    vector <int> arr;
    vector <int> mission;
    int x,mission=0;
    for(int i=0; i<n; i++)
    {
        scanf("%d",&x);
        arr.push_back(x);

        if(x == 4 && mission[] == 0)
        {

        }

    }

    int ans = 0;
    printf("%d",ans);

    return 0;
}
